{
  "name": "truewords-review-pwa",
  "version": "0.5.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/build-worker.mjs",
    "check": "node --check server-sync.js && node --check admin-upload.js && node --check analysis-import.js && node --check login.js && node --check account-setup.js && node --check pilot-v2.js && node --check upload.js && node --check truewords-ui-theme.js && node --check review-cross-owner-fix.js && node --check review-precision.js && node --check review.js && node --check review-boundaries.js && node --check scripts/build-worker.mjs && node --check scripts/import-review-data.mjs && node --check scripts/segment-pilot-v3.mjs && tsc --noEmit",
    "dev": "npm run build && wrangler dev",
    "deploy": "npm run build && wrangler deploy",
    "import:data": "node scripts/import-review-data.mjs",
    "segment:v3": "node scripts/segment-pilot-v3.mjs",
    "db:migrate:local": "wrangler d1 migrations apply truewords-review-sync-db --local",
    "db:migrate:remote": "wrangler d1 migrations apply truewords-review-sync-db --remote"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "latest",
    "typescript": "^5.9.3",
    "wrangler": "^4.92.0"
  }
}
